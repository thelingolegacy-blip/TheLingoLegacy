# Lingo Legacy Asset Production Map

This tree mirrors the Phase 2 production architecture. Store source assets here during development, then publish optimized production assets through the approved storage and release workflow.

Pipeline: create asset -> quality check -> add metadata -> upload storage -> connect database -> publish -> promote -> track results.
