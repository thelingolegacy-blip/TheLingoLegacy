FROM ubuntu:24.04
ARG RUNNER_VERSION=2.337.0
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates curl git jq sudo libicu74 libssl3 zlib1g libkrb5-3 libgcc-s1 libstdc++6 && rm -rf /var/lib/apt/lists/*
RUN useradd --create-home --shell /bin/bash runner && mkdir -p /opt/actions-runner && chown -R runner:runner /opt/actions-runner
WORKDIR /opt/actions-runner
RUN curl -fsSL -o actions-runner.tar.gz https://github.com/actions/runner/releases/download/v${RUNNER_VERSION}/actions-runner-linux-x64-${RUNNER_VERSION}.tar.gz && tar xzf actions-runner.tar.gz && rm actions-runner.tar.gz && ./bin/installdependencies.sh
COPY entrypoint.sh /entrypoint.sh
RUN chmod 0755 /entrypoint.sh && chown runner:runner /entrypoint.sh
USER runner
ENTRYPOINT ["/entrypoint.sh"]
