import { Card, Section } from '@/components';

export default function AdminLogs() {
  return (
    <Section title="Admin Logs">
      <Card>
        <p>Operational logs and telemetry summaries will render here once observability is connected.</p>
      </Card>
    </Section>
  );
}
