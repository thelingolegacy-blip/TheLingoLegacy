import { DevelopmentSpecPanel } from '@/components/RolloutPanels';

export default function DevelopmentSpecPage() {
  return <><section className="page-shell shell"><p className="eyebrow">Phase 8 Full Development Build Specification</p><h1>Exact pages, components, data, APIs, deployment, Firebase, and launch checklist.</h1><p className="hero-copy">This turns the rollout blueprint into engineering targets for the next implementation wave.</p></section><DevelopmentSpecPanel /></>;
}
