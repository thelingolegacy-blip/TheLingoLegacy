import { Card, Section } from '@/components';

export default function DailyGameShell() {
  return (
    <Section title="Daily Lingo Game Shell">
      <Card>
        <p>The playable app route is reserved for the migrated Lingo game experience.</p>
      </Card>
    </Section>
  );
}
