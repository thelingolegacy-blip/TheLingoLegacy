import { Card, Section } from '@/components';

export default function WalletHistory() {
  return (
    <Section title="Wallet History">
      <Card>
        <p>Wallet events will stream here once the production wallet provider and audit store are connected.</p>
      </Card>
    </Section>
  );
}
