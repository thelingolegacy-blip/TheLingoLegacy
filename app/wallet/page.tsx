import { Card, Section } from '@/components';

export default function Wallet() {
  return (
    <Section title="Wallet Dashboard">
      <Card>
        <p>Wallet initiation, callback state, event audit, and balance integrations connect here after credentials are configured.</p>
      </Card>
    </Section>
  );
}
