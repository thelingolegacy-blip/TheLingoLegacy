# API Contracts

Planned endpoints:
- /api/waitlist
- /api/notifications
- /api/surveys
- /api/support
- /api/assets
- /api/campaigns
- /api/rewards
- /api/profile

Each endpoint must define auth, request body, response shape, validation, rate limits, analytics event, and admin visibility.
